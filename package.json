{
  "name": "magic-code",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "check": "bun run typecheck && bun run test",
    "prepare": "git config core.hooksPath .githooks || true",
    "ui": "bun packages/app/scripts/ui.ts"
  },
  "dependencies": {
    "@magic/app": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
