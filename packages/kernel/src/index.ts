/**
 * `@magic/kernel` —— 内核入口。
 *
 * 内核＝让 Agent 独立运转的那部分：主循环 / 工具机制 / 权限 / 上下文 /
 * 会话记录 / 事件流 / 模型接缝（技术方案 · 分层）。
 *
 * 两条纪律（技术方案 · 代码治理）：
 * - 零外壳依赖 —— 内核不 import 外壳（`@magic/tui` / `@magic/app`）；
 * - 不直碰文件系统 —— 文件操作一律经沙箱。
 *
 * U01 只落包骨架；单元落位见 工作分解 · 阶段 1：
 * 记录 U02 · 接缝 U03 · 循环 U04 · 沙箱 U05 · 工具 U06 · 权限 U07 ·
 * 控制面 U08 · 提示词 U10。
 */
export * from './contracts/index.ts'
