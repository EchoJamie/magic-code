/**
 * `@magic/tui` —— 外壳（显示组件自持）。
 *
 * **只认控制面**——经 `ControlTransport`（外壳侧一端，取自 `@magic/contracts`）发命令 /
 * 订阅事件；不认知任何域、不 import 具体实现（技术方案 · 边界纪律）。
 * 渲染按 `KernelEvent` 的 `kind` **自动收窄**（判别联合视图——无须强转）。
 *
 * 许可外部库：Ink（显示组件自建——技术方案 · 选型）。
 *
 * 骨架占位——TUI 骨架见 工作分解 · U09（显示打磨 U20 · 性能 U21）。
 */
export {}
